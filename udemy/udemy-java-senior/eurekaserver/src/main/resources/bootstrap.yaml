spring:
  application:
    name: eurekaserver
eureka:
  instance:
    prefer-ip-address: true
  client:
    registerWithEureka: false
    fetchRegistry: false

server:
  port: 8070

management:
  endpoints:
    web:
      exposure:
        include: "*"